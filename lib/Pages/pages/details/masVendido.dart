import 'package:flutter/material.dart';

class MasVendido extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('masVendido'),
      ),
    );
  }
}
